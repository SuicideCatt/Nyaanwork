set(NYAANWORK_INPUT FALSE CACHE BOOL "Enable Input module" FORCE)

set(NYAANWORK_INPUT_COMBINATION_SIZE 0 CACHE INTERNAL
	"Input key and buttons combination size" FORCE)
set(NYAANWORK_INPUT_COMBINATIONS_COUNT 0 CACHE INTERNAL
	"Input Action key and buttons combinations count" FORCE)

if (CONFIG_NYAANWORK_INPUT)
	set(NYAANWORK_INPUT TRUE CACHE BOOL "Enable Input module" FORCE)

	set(NYAANWORK_INPUT_COMBINATION_SIZE
		${CONFIG_NYAANWORK_INPUT_COMBINATION_SIZE} CACHE INTERNAL
		"Input key and buttons combination size" FORCE)
	set(NYAANWORK_INPUT_COMBINATIONS_COUNT
		${CONFIG_NYAANWORK_INPUT_COMBINATIONS_COUNT} CACHE INTERNAL
		"Input Action key and buttons combinations count" FORCE)
endif()
